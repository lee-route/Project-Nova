# 보스 패턴 팀 — 음성 상쇄 연동 안내

> 음성 인식·STT·명령 파싱·상쇄 **판정**은 C++로 구현되어 있습니다.  
> **스킬·연출·모션·이펙트**는 `BP_NovaPlayerController` 등 **블루프린트**에서 진행합니다.  
> 보스 패턴 팀은 **패턴 시작 시 API 1개만 호출**하면 됩니다.  
> 작업 브랜치: `현진`

---

## C++ vs 블루프린트 (역할 분담)

| C++ (완료·유지) | 블루프린트 (연출 담당) |
|-----------------|------------------------|
| 음성 인식, STT, 명령 파싱 | 무기 장착 몽타주, 메시 전환 |
| `OpenBossCounterWindow()` | 상쇄 성공 VFX·카메라 연출 |
| 상쇄 성공 **판정** | "도와줘" 동료 연출 |
| `GetEquippedSecondaryWeapon()` | 패턴별 스킬 이펙트 |

---

## 한 줄 요약

**보스 패턴이 시작될 때 `OpenBossCounterWindow(패턴타입)` 한 번 호출.**

---

## 꼭 해야 할 것

각 보스 패턴 **텔레그래프 / 공격 시작 시점**에 아래 호출:

```
OpenBossCounterWindow(패턴타입)
```

- **호출 주체:** `NovaClickMovePlayerController`
- **BP 호출:** 가능 (`BlueprintCallable`)

---

## 패턴별 호출 값

| 보스 패턴 | 호출할 값 | 플레이어가 말해야 하는 무기 |
|-----------|-----------|----------------------------|
| 레이저 | `LaserShield` | **방패** |
| 공간절단 | `SpaceScythe` | **낫** |
| 소환 | `SummonBow` | **활** |
| 잔해 | `DebrisHammer` | **검** |

---

## 호출하면 자동으로 되는 것 (C++ — 보스팀 추가 작업 불필요)

1. **약 1.2초** 상쇄 윈도우가 열림 (`CounterWindowSeconds`, 설정으로 변경 가능)
2. 그 안에 플레이어가 **해당 무기명**을 말하면 상쇄 **성공 판정**
3. 무기 **상태** 전환 + `Boss counter success` 디버그 메시지
4. BP **`OnBossCounterVisualSuccess`** 호출 → 연출·VFX는 BP에서 처리
5. 윈도우 시간이 지나면 자동으로 닫힘 (`Counter window missed`)

---

## BP에서 호출 예시

```
Get Player Controller
→ Cast to NovaClickMovePlayerController
→ Open Boss Counter Window
   - Counter Type: LaserShield / SpaceScythe / SummonBow / DebrisHammer
```

---

## 연동 전 테스트 (보스 코드 없이)

| 키 | 테스트 내용 |
|----|-------------|
| **F5** | 레이저 상쇄 윈도우 (`LaserShield`) |
| **F6** | 공간절단 (`SpaceScythe`) |
| **F7** | 소환 (`SummonBow`) |
| **F8** | 잔해 (`DebrisHammer`) |
| **1~4** | 무기 전환 (검 / 활 / 낫 / 방패) |

**예:** F5(레이저) 누른 뒤 **4**(방패) 또는 **"방패"** 말하기 → `Boss counter success` 표시되면 정상.

| F키 | 필요 무기 | 키보드 |
|-----|-----------|--------|
| F5 | 방패 | **4** |
| F6 | 낫 | **3** |
| F7 | 활 | **2** |
| F8 | 검 | **1** |

---

## 보스팀이 하지 않아도 되는 것

- Azure Speech / 마이크 / STT 설정 (C++)
- 음성 명령 파서 (C++)
- `VoiceCaptureComponent` 설정 (C++)
- 스킬·연출·모션·이펙트 (BP — 별도 담당)

---

## 성공 / 실패 확인

| 화면 메시지 | 의미 |
|-------------|------|
| `Counter window open: N` | 상쇄 윈도우 열림 |
| `Boss counter success: ...` | 올바른 무기로 상쇄 성공 |
| `Counter window requires command ...` | 윈도우 중 잘못된 무기 → 거절 |
| `Counter window missed` | 시간 내 입력 없음 |

---

## (선택) 상쇄 성공 후 보스 반응

**연출·VFX:** `BP_NovaPlayerController` → **`OnBossCounterVisualSuccess`** 에 몽타주·Niagara 연결.

**보스 AI 반응:** `NovaCombatVoiceGateComponent` → **`OnCounterSucceeded`** 에 넉백·패턴 캔슬 등 연결 (BP 또는 C++).

---

## 작업 시 주의

| 항목 | 내용 |
|------|------|
| 브랜치 | `git pull origin 현진` |
| PlayerController | `BP_NovaPlayerController`가 `NovaClickMovePlayerController` 자식이어야 함 |
| 상쇄 윈도우 길이 | 기본 1.2초 — `Config/DefaultNovaVoice.ini`의 `CounterWindowSeconds` |

---

## 관련 코드

| 파일 | 역할 |
|------|------|
| `NovaClickMovePlayerController.*` | `OpenBossCounterWindow()` 제공 |
| `NovaCombatVoiceGateComponent.*` | 상쇄 윈도우·성공 판정 |
| `NovaVoiceTypes.h` | `ENovaBossCounterType`, `ENovaVoiceCommand` |

---

## 참고 문서

- [음성인식_남은작업_정리.md](./음성인식_남은작업_정리.md) — 전체 음성 작업 개요
