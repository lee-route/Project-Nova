# Project-Nova 음성 인식 — 남은 작업 정리

> 보스 AI는 다른 팀 담당. 보스 패턴(신호)은 받았다는 전제.
> Azure 키 설정(`Config/LocalNovaVoice.ini`)은 완료된 상태.
> 작업 브랜치: `현진`

---

## 역할 분담 (팀 합의)

| 레이어 | 담당 | 내용 |
|--------|------|------|
| **C++** | 음성 인식·게임 로직 | 마이크 → STT → 명령 파싱, 무기 **상태**, 보스 상쇄 **판정**, `OpenBossCounterWindow()` API |
| **블루프린트** | 스킬·연출·모션·이펙트 | 무기 메시/애니 전환, 상쇄 성공 VFX·몽타주, 동료 도움 연출 등 **비주얼만** |

C++는 로직과 상태만 처리합니다. 화면에 뜨는 디버그 메시지는 개발용이며, **실제 연출은 BP에서** `OnSecondaryWeaponChanged` / `OnBossCounterVisualSuccess` 등 이벤트에 구현합니다.

| 구분 | 내용 |
|------|------|
| **인식 확인 (C++)** | 말하면 인식되는지, 지연·정확도 튜닝 |
| **보스 연동 (C++ API + BP 호출)** | 패턴 시작 시 `OpenBossCounterWindow()` |
| **연출 (BP)** | 무기·상쇄·스킬의 모션·이펙트·사운드 |

---

## 이미 완료된 것

| 항목 | 상태 |
|------|------|
| 마이크 → VAD → Azure STT | 완료 |
| 명령 파서 (활/방패/낫/검 + STT 오인식 별칭) | 완료 |
| Azure NBest 후보 선택 (1순위 한글 무기 우선, 검→활 오인식 방지) | 완료 |
| 보스 상쇄 게이트 (`NovaCombatVoiceGateComponent`) | 완료 |
| `OpenBossCounterWindow()` API | 완료 |
| `NovaClickMovePlayerController` 연동 | 완료 |
| 키보드 백업 (1~4 무기, F5~F8 상쇄 테스트) | 완료 |
| Azure 키 | 완료 |
| 디버깅·빌드 검증 | 완료 |
| Git 커밋·푸시 (`현진`, `LocalNovaVoice.ini` 제외) | 완료 |

---

## 앞으로 할 작업

| # | 작업 | 설명 |
|---|------|------|
| 1 | **PIE 음성 테스트** | `방패`, `활`, `낫`, `검` 인식 확인 (최근 PIE: 4종 모두 성공, 검 간헐 no match → 정규화 보강) |
| 2 | **튜닝** | VAD 임계값, 신뢰도(0.40), 지연(ms) 확인 |
| 3 | **보스 패턴 연결** | 패턴 시작 시 `OpenBossCounterWindow()` 호출 |
| 4 | **통합 테스트** | 보스 패턴 4종 × 음성 상쇄 동작 확인 |
| 5 | **무기·상쇄 연출 (BP)** | C++ 이벤트에 몽타주·Niagara·무기 메시 전환 연결 (**C++ 추가 작업 없음**) |

---

## 핸드오프 (1·2번 끝나면 전달)

1. PIE에서 `방패/활/낫/검` 인식 **되는지** (스크린샷 또는 짧은 영상)
2. 평균 지연 — 화면의 `STT req=... avg=...ms` 값
3. 튜닝했으면 변경한 값 (VAD 임계값, 신뢰도 등)

---

## 연동 API

### 보스 패턴 연결 (3번)

패턴 시작 시 아래만 호출:

```
OpenBossCounterWindow(패턴타입)
```

| 보스 패턴 | 호출 값 | 필요한 무기 |
|-----------|---------|-------------|
| 레이저 | `LaserShield` | 방패 |
| 공간절단 | `SpaceScythe` | 낫 |
| 소환 | `SummonBow` | 활 |
| 잔해 | `DebrisHammer` | 검 |

### 무기·상쇄 연출 (5번 — **블루프린트**)

`BP_NovaPlayerController`에서 아래 **BlueprintImplementableEvent**에 연출을 연결합니다.

| BP 이벤트 | 호출 시점 | 연출 예시 |
|-----------|-----------|-----------|
| `OnSecondaryWeaponChanged` | 무기 상태가 바뀔 때 (음성·키보드 공통) | 무기 메시 교체, 장착 몽타주 |
| `OnBossCounterVisualSuccess` | 보스 상쇄 성공 판정 직후 | 패턴별 VFX, 카메라 셰이크 |
| `OnCompanionHelpVisualRequested` | "도와줘" 인식 시 | 동료 소환·UI 연출 |

| C++ API (상태 읽기/테스트용) | 용도 |
|--------------|------|
| `GetEquippedSecondaryWeapon()` | 현재 무기 enum |
| `SwitchSecondaryWeapon()` | BP에서 수동 전환 테스트 |

### 음성 없이 테스트 (연동 전)

| 키 | 동작 |
|----|------|
| F5 | 레이저 상쇄 윈도우 열기 |
| F6 | 공간절단 상쇄 윈도우 열기 |
| F7 | 소환 상쇄 윈도우 열기 |
| F8 | 잔해 상쇄 윈도우 열기 |
| 1~4 | 검 / 활 / 낫 / 방패 (키보드 무기 전환) |

---

## 3번부터 시작해도 되는가?

**연결 작업만 보면 1·2번과 병행 가능합니다.**

- 보스 연결은 음성 인식 없이 **F5~F8 + 1~4** 키보드로 먼저 검증 가능
- 보스팀이 패턴을 준비해 두었다면 **1~2시간** 안에 3번 연결 가능
- 4번(통합 테스트)은 1·2번 결과가 나온 뒤 하는 게 안전

### 1·2번(인식 확인)이 늦어질 때 리스크

| 상황 | 영향 |
|------|------|
| PIE 음성 테스트 미완 | 4번에서 마이크·Azure 문제가 한꺼번에 터질 수 있음 |
| 튜닝 미완 | 상쇄 윈도우 안에서 오인식·지연 → 4번 재작업 |

---

## 추천 진행 순서

**1(10~15분 스모크) → 2 → 3 → 4 → 5**

| 진행 방식 | 속도 | 안전 |
|-----------|------|------|
| 1→2 완료 후 3→4→5 | 보통 | **높음 (권장)** |
| 3 먼저 + 1·2 병행 | 빠름 | 적당 |
| 1·2 생략, 3부터 | 가장 빠름 | 시연 직전 리스크 큼 |

---

## 작업 시 주의

| 항목 | 내용 |
|------|------|
| 브랜치 | `현진` pull 후 작업 |
| Azure 키 | `LocalNovaVoice.ini`는 Git에 없음 — 각자 `LocalNovaVoice.ini.example` 복사 후 키 입력 |
| PlayerController | `BP_NovaPlayerController`가 `NovaClickMovePlayerController` 자식이어야 음성 동작 |
| PIE 확인 | 팝업에 `Voice: listening` 표시되는지 확인 |

---

## 하루 목표 vs 범위 밖

### 하루에 가능

- 음성 무기 전환 동작
- 보스 패턴 → 음성 상쇄 연결 (패턴만 받으면)
- 4패턴 통합 시연

### 하루에 어려운 것 (범위 밖)

- 보스 AI 패턴 자체 구현 (다른 팀)
- **스킬·연출·모션·이펙트 BP 구현** (C++ 담당 아님 — BP 팀)
- 오프라인 Whisper 전환
- Azure 지연이 500ms 넘을 때 전면 재설계

---

## 무선 헤드셋 / 마이크 실패 시

- Windows 입력 장치를 **Hands-Free / AG Audio** 로 선택 (Stereo는 마이크 없음)
- 실패 시 PIE 화면에 **감지된 마이크 목록** 표시
- `LocalNovaVoice.ini`에 `CaptureDeviceName=Hands-Free` 등 부분 이름 지정 가능
- 코드가 장치 **기본 샘플레이트**로 캡처 후 **16kHz로 리샘플** (무선 호환 개선)
- 그래도 안 되면 키보드 1~4 / F5~F8로 테스트, 발표용 USB 마이크 권장

---

## STT 오인식 별칭 (파서)

Azure가 잘못 텍스트로 바꿔도 아래처럼 **무기 명령으로 매핑**합니다.

| 무기 | 정식 | STT 오인식 예 | PIE 확인 (최근) |
|------|------|----------------|-----------------|
| 검 | 검, 칼 | **건**, **거**, **금**, **컴**, com, 점, ken, gem, ㄱ+ㅓ 음절 | `검.`/`컴.` → 검(4) |
| 활 | 활, 궁 | **팔**, **괄**, **핀**, 곽, 화, 발 | `활.` 성공(0.79~0.95) |
| 낫 | 낫 | **낮**, **나**, 낳, nat, nit, 넛, 랫 | `나.`/`낫.` 성공(0.18~0.20) |
| 방패 | 방패 | 방, 빵패, 반패 | `방패.` 성공(0.81~0.88) |

`Voice STT fail: Empty` 는 STT가 텍스트를 못 뽑은 경우 → 말을 조금 길게·또렷하게.
거절 로그에 `hangul=...` 가 보이면 파서 매칭 실패(정규화·별칭 추가 대상).

무기 명령(낫/활/검/방패)은 Azure 신뢰도가 낮아도 **0.05 이상**이면 통과 (`MinWeaponCommandConfidence`, 직접 한글 매칭은 더 낮게 허용).

성공 로그 예: `Voice command: 검. -> 검(4) (0.95, 250ms)` — 괄호 안 숫자는 `ENovaVoiceCommand` enum 값(검=Sword=4, 활=Bow=1, 낫=Scythe=3, 방패=Shield=2).

**NBest 선택 규칙:** STT 1순위가 `검/활/낫/방패` 등 직접 한글이면 그 결과를 신뢰합니다. 1순위가 무기가 아닐 때만 NBest 후보를 스캔합니다. 1순위가 `나`이고 후보에 `낫`이 있으면 `낫`으로 보정합니다.

---

## 관련 문서

- [보스패턴팀_연동_안내.md](./보스패턴팀_연동_안내.md) — 보스 패턴 팀 연동
- [Azure Speech 설정 방법.txt](./Azure%20Speech%20설정%20방법.txt) — Azure 키·PIE 테스트 (키는 Git 미포함)

## 관련 파일

| 파일 | 역할 |
|------|------|
| `Source/NovaUproject/NovaVoiceCaptureComponent.*` | 마이크 + VAD + STT |
| `Source/NovaUproject/NovaAzureSpeechClient.*` | Azure REST 클라이언트 |
| `Source/NovaUproject/NovaVoiceCommandParser.*` | 명령어 파서 |
| `Source/NovaUproject/NovaVoiceWeaponLexicon.*` | 무기 키워드·한글 직접 매핑·NBest 선택 |
| `Source/NovaUproject/NovaCombatVoiceGateComponent.*` | 보스 상쇄 게이트 |
| `Source/NovaUproject/NovaClickMovePlayerController.*` | 음성·키보드 연동 |
| `Config/LocalNovaVoice.ini` | Azure 키 (Git 제외) |
| `Config/LocalNovaVoice.ini.example` | 키 설정 예시 |
| `Config/DefaultNovaVoice.ini` | 기본 설정 |

---

## Azure 키 보안

- `LocalNovaVoice.ini`는 `.gitignore`에 포함 — **절대 GitHub에 올리지 않기**
- 키가 채팅·공개 저장소에 노출됐다면 Azure 포털에서 **키 재생성** 권장
- API 호출에는 **키 1**만 사용 (키 2는 백업)
