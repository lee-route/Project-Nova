# Project-Nova 음성 인식 — 남은 작업 정리

> 보스 AI는 다른 팀 담당. 보스 패턴(신호)은 받았다는 전제.
> Azure 키 설정(`Config/LocalNovaVoice.ini`)은 완료된 상태.

---

## 이미 완료된 것

| 항목 | 상태 |
|------|------|
| 마이크 → VAD → Azure STT | 완료 |
| 명령 파서 (활/방패/낫/망치/도와줘/취소) | 완료 |
| 보스 상쇄 게이트 (`NovaCombatVoiceGateComponent`) | 완료 |
| `OpenBossCounterWindow()` API | 완료 |
| `NovaClickMovePlayerController` 연동 | 완료 |
| 키보드 백업 (1~4 무기, F5~F8 상쇄 테스트) | 완료 |
| Azure 키 | 완료 |
| 디버깅·빌드 검증 | 완료 |
| Git 커밋·푸시 (`현진`, `LocalNovaVoice.ini` 제외) | 완료 |

---

## 곧 할 예정 (인식 확인)

1. **PIE 음성 테스트** — `방패`, `활`, `낫`, `망치` 인식 확인
2. **튜닝** — VAD 임계값, 신뢰도(0.55), 지연(ms) 확인

---

## 남은 작업

1. **보스 패턴 연결** — 패턴 시작 시 `OpenBossCounterWindow()` 호출
2. **통합 테스트** — 보스 패턴 4종 × 음성 상쇄 동작 확인
3. **무기 전환 피드백** — 화면 표시 또는 BP 이벤트로 비주얼 팀에 넘기기

---

## 1번부터 시작해도 되는가?

**연결 작업만 보면 더 빨리 끝납니다.**

보스 연결은 음성 인식 없이도 먼저 할 수 있습니다.

- 보스 패턴 시작 → `OpenBossCounterWindow(타입)` 호출만 붙이면 됨
- 테스트는 **F5~F8**(상쇄 윈도우) + **1~4**(무기) 키보드로 가능
- 보스팀이 패턴을 준비해 두었다면 **1~2시간** 안에 연결 가능

### 인식 확인(곧 할 예정)을 생략할 때 리스크

| 생략한 것 | 나중에 터질 수 있는 것 |
|-----------|------------------------|
| PIE 음성 테스트 | Azure 키·마이크 문제를 늦게 발견 |
| 튜닝 | 상쇄 윈도우 안에서 음성 지연·오인식 → 통합 테스트에서 재작업 |

---

## 추천 진행 (가장 빠른 안전 루트)

**인식 확인 1(10~15분 스모크만) → 남은 작업 1 → 2 → 인식 확인 2(필요 시)**

| 진행 방식 | 속도 | 안전 |
|-----------|------|------|
| 인식 확인 → 남은 작업 순서대로 | 보통 | 높음 |
| **인식 확인 1(짧게) → 남은 작업 1 → 2 → 인식 확인 2** | **가장 빠름** | 적당 |
| 인식 확인 생략, 남은 작업 1부터 | 연결만 빠름 | 시연 직전 리스크 큼 |

---

## 보스팀이 넘겨줘야 하는 것 (최소)

패턴 시작 시 아래만 호출하면 됩니다.

```
OpenBossCounterWindow(패턴타입)
```

| 보스 패턴 | 호출 값 | 필요한 무기 |
|-----------|---------|-------------|
| 레이저 | `LaserShield` | 방패 |
| 공간절단 | `SpaceScythe` | 낫 |
| 소환 | `SummonBow` | 활 |
| 잔해 | `DebrisHammer` | 망치 |

### 디버그 키 (보스 연결 전 테스트용)

| 키 | 동작 |
|----|------|
| F5 | 레이저 상쇄 윈도우 열기 |
| F6 | 공간절단 상쇄 윈도우 열기 |
| F7 | 소환 상쇄 윈도우 열기 |
| F8 | 잔해 상쇄 윈도우 열기 |
| 1~4 | 방패 / 낫 / 활 / 망치 (키보드 무기 전환) |

---

## 하루 목표 vs 범위 밖

### 하루에 가능

- 음성 무기 전환 동작
- 보스 패턴 → 음성 상쇄 연결 (패턴만 받으면)
- 4패턴 통합 시연

### 하루에 어려운 것 (범위 밖)

- 보스 AI 패턴 자체 구현 (다른 팀)
- 무기 3D 모델·이펙트·애니 완성 (에셋/애니 팀)
- 오프라인 Whisper 전환
- Azure 지연이 500ms 넘을 때 전면 재설계

---

## 관련 파일

| 파일 | 역할 |
|------|------|
| `Source/NovaUproject/NovaVoiceCaptureComponent.*` | 마이크 + VAD + STT |
| `Source/NovaUproject/NovaAzureSpeechClient.*` | Azure REST 클라이언트 |
| `Source/NovaUproject/NovaVoiceCommandParser.*` | 명령어 파서 |
| `Source/NovaUproject/NovaCombatVoiceGateComponent.*` | 보스 상쇄 게이트 |
| `Source/NovaUproject/NovaClickMovePlayerController.*` | 음성·키보드 연동 |
| `Config/LocalNovaVoice.ini` | Azure 키 (Git 제외) |
| `Config/DefaultNovaVoice.ini` | 기본 설정 |

---

## Azure 키 보안

- `LocalNovaVoice.ini`는 `.gitignore`에 포함 — **절대 GitHub에 올리지 않기**
- 키가 채팅·공개 저장소에 노출됐다면 Azure 포털에서 **키 재생성** 권장
- API 호출에는 **키 1**만 사용 (키 2는 백업)
