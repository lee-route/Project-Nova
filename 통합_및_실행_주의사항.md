# Project-Nova 통합 및 실행 주의사항

> **대상:** `main` / `현진` 브랜치 최신 pull 후 작업하는 팀원  
> **목적:** 음성 인식(현진) + 규민 던전/NPC 에셋 통합 상태를 공유하고, 크래시·누락을 줄이기 위한 체크리스트

---

## 1. 받는 방법

```bash
git checkout main
git pull origin main
git lfs pull
```

- **`git lfs pull`은 필수입니다.** 실행하지 않으면 `.uasset` / `.umap`이 131바이트 포인터만 남아 맵·BP가 깨집니다.
- Azure Speech 키는 Git에 없습니다. `Config/LocalNovaVoice.ini.example`을 참고해 `Config/LocalNovaVoice.ini`를 로컬에만 만드세요.

---

## 2. 프로젝트 실행

- **실행 파일:** 루트의 `NovaUproject.uproject` (기존과 동일)
- **엔진:** UE 5.7
- **기본 시작 맵:** `/Game/ThirdPerson/Lvl_ThirdPerson`
- **기본 GameMode:** `NovaGameMode` (C++) → `BP_NovaPlayerController` (음성·QWER 스킬)

---

## 3. 브랜치별로 들어온 작업

| 구분 | 담당/출처 | 내용 |
|------|-----------|------|
| **음성·상쇄·무기** | 현진 | C++ STT/파서, `NovaGameMode`, `BP_NovaPlayerController`, Hammer enum(BP 호환) |
| **던전·NPC** | 규민 (선택 import) | `Content/Fantastic_Dungeon_Pack/` — 맵, `bp_npc`, 대화 UI, 포털 등 |
| **로컬 전용** | — | `_local-only/` (PDF·회의록 등, Git 제외) |

---

## 4. ⚠️ 꼭 알아둘 것 (규민 에셋)

### 4-1. 가져온 것

- `Content/Fantastic_Dungeon_Pack/` (던전 맵 5개 + 오버뷰 2개, NPC BP, 대화 위젯 등)
- `Config/Localization/` (대화/로컬라이제이션 설정)

### 4-2. 아직 가져오지 **않은** 것

- **`Content/ParagonSevarog/`** (및 기타 Paragon 캐릭터 팩 전체)

**던전 맵 1, 2, 3, 5**는 맵 안에서 **`/Game/ParagonSevarog`** 를 참조합니다.  
ParagonSevarog가 없으면:

- 맵은 열릴 수 있으나 **보스/몬스터 메시 Missing Reference** 가능
- Output Log에 `Failed to load` / Paragon 경로 오류 가능

**맵 4** (`map_dungeon_level_4_temple`)는 Paragon 참조가 없어 **상대적으로 안전**합니다.

→ 팀원 PC에서 몬스터·보스가 정상이었다면, **규민 브랜치의 ParagonSevarog 추가 import**가 필요할 수 있습니다. (필요 시 현진/담당자에게 요청)

### 4-3. 던전 맵에서 음성 쓰려면

던전 맵은 규민 쪽 GameMode 설정이 다를 수 있습니다.  
**World Settings → GameMode Override** 를 `NovaGameMode`(또는 `BP_NovaPlayerController` 사용 GameMode)로 맞춰야 음성·QWER·상쇄가 동작합니다.

---

## 5. 음성·전투 테스트 (ThirdPerson 기준)

| 키/입력 | 동작 |
|---------|------|
| `1~4` | 검 / 활 / 낫 / 방패 |
| `Q~R` | 현재 무기 스킬 (`BP_UseSkillQ/W/E/R`) |
| `F5~F8` | 보스 상쇄 윈도우 테스트 (방패/낫/활/검) |
| 음성 | `방패`, `활`, `낫`, `검` 등 |

상세: [보스패턴팀_연동_안내.md](./보스패턴팀_연동_안내.md), [음성인식_남은작업_정리.md](./음성인식_남은작업_정리.md)

---

## 6. 던전·NPC 테스트

Content Browser에서 예:

- `/Game/Fantastic_Dungeon_Pack/maps/map_dungeon_level_1_dungeon`
- `/Game/Fantastic_Dungeon_Pack/maps/map_dungeon_level_4_temple`
- `/Game/Fantastic_Dungeon_Pack/maps/map_dungeon_level_5_bossroom`

Play 후 **Output Log**에서 `Paragon`, `Failed to load` 검색.

---

## 7. C++ 빌드 시

- UE 에디터가 켜져 있으면 **Live Coding** 때문에 커맨드라인 빌드가 실패할 수 있습니다.
- **에디터 종료** 후 빌드하거나, 에디터에서 **Ctrl+Alt+F11** 사용.

---

## 8. Git에 올리지 말 것

- `Config/LocalNovaVoice.ini` (Azure 키)
- `_local-only/` (참고 PDF·회의록)
- `_import_tmp/`

---

## 9. 문제 발생 시 체크 순서

1. `git lfs pull` 했는지  
2. `LocalNovaVoice.ini` / 마이크 권한  
3. GameMode가 `NovaGameMode` + `BP_NovaPlayerController`인지  
4. 던전 맵 Missing → **ParagonSevarog** 필요 여부  
5. BP 컴파일 오류 → `ENovaVoiceCommand`는 **Hammer = 검(Sword 표시명)**

---

## 10. 관련 문서

- [보스패턴팀_연동_안내.md](./보스패턴팀_연동_안내.md)
- [음성인식_남은작업_정리.md](./음성인식_남은작업_정리.md)
