================================================================================
  Project-Nova / models/npc-test — 설계·진행 통합 문서 (ENGINE-DESIGN)
  갱신: 2026-06-08
  브랜치: main (기현과 동일 커밋 11f385e 반영)
  UE 연동 실무: INTEGRATION-GUIDE.txt  ◄ UE 팀 1순위
  HTTP 계약 JSON: GAME-API.json
================================================================================


1. 한 줄 요약
--------------------------------------------------------------------------------
  밀수 화물 퀘스트 1본 + 결정론 엔진 + 파서 분리 + HTTP API v1 + 회귀·스모크 검증 완료.
  main 반영 완료 — 핸드오프 시점. 다음 마일스톤: UE Blueprint turn-in 연동.
  서버 실행: run-api.cmd 또는 node npc-api-server.mjs


2. 진행률 (2026-06-05 기준)
--------------------------------------------------------------------------------
  npc-test 엔진·API (본인 담당)     약 85~90%  — 핸드오프 가능
  팀 목표 (UE에서 1세로 플레이)      약 50~55% — UE turn-in만 남음
  최종 비전 (v2·LLM HTTP·다퀘스트)  약 35~40%

  LLM API 결제만으로는 완성 아님. turn-in·분기는 LLM 없이 동작.
  LLM은 대사 연출 레이어 (브라우저 mock/live 경로는 있음, HTTP v2 대기).


3. 시스템 개요
--------------------------------------------------------------------------------
  Project-Nova NPC 정보 시스템의 검증용 샌드박스.
  언리얼 본편에 붙일 "두뇌"를 브라우저·Node·HTTP로 먼저 증명한다.

  브라우저 + Node + HTTP 프로토타입.
  Unreal Engine(nova/)은 별도 — v1은 로컬 API 서버에 HTTP로 붙인다.

  목표: 동일 입력·동일 NPC 스탯 → 동일 interpretedFacts → 동일 퀘스트 분기.

  현재 상태 (2026-06-05):
    - 엔진·API v1·회귀 테스트 완료, main 반영 (11f385e)
    - nova/ UE 연동 코드 0건 — INTEGRATION-GUIDE.txt 로 핸드오프
    - LLM live는 브라우저 경로만; HTTP /v1/dialogue 는 v2


4. 설계 원칙 (불변)
--------------------------------------------------------------------------------
  (1) 결정론: 동일 facts + 동일 관계·평판·quantityMode → 동일 applied_rules
  (2) NPC 진실: LLM·퀘스트 판정은 interpretedFacts 만 사용 (원문·parser fact 직접 금지)
  (3) LLM: 말투·연출만; [Grounded DATA] 밖 사실 생성 금지
  (4) 퀘스트 분기: quests-draft outcomes[] (authority / black_market / fallback)
  (5) 평판 이중 구조:
        - 주제별 reputation (npcs.json) → 왜곡 엔진
        - 플레이어↔NPC reputation (reputation-config) → tier 대사·trust 보정


5. 마일스톤 타임라인
--------------------------------------------------------------------------------
  [완료] 늑대 퀘스트 제거 → quest_abandoned_cargo 단일 정식화
  [완료] 평판 tier·의뢰인별 수락 대사·outcome 분기 (authority / black_market)
  [완료] 로컬 QuestGameState·Quest Playtest UI
  [완료] 지적 사항 4건 — WorldTruth/PKB, GameClock, source_chain, LlmFactAnchor
  [완료] npc-parser.js 분리 (app.js = UI만)
  [완료] API 연동 전 체크리스트 7항 + npc-api-server.mjs v1
  [완료] mock_fallback 다양화 (anchor 실패 연출)
  [완료] GET /v1/quest/meta, api-smoke, run-api.cmd/ps1, API-QUICKSTART.md
  [완료] run-all-tests 15 suites + api-smoke HTTP 검증
  [완료] main 브랜치 fast-forward 머지·push (커밋 11f385e)
  [완료] INTEGRATION-GUIDE.txt (UE 팀 연동 가이드)
  [진행 예정] nova/ UE Blueprint HTTP 클라이언트
  [대기] API v2 — LLM proxy, 기만→outcome, runQuestFlow HTTP


6. 데이터 파이프라인
--------------------------------------------------------------------------------

  [플레이어 입력]
       scenarioText (한국어)
            │
            ▼
  npc-parser.js ──► facts[] (FactInput)
            │
            ▼
  quest-system.executeScenario / propagateFactBundle
    · 발신: 플레이어(usePlayerAsSender) 또는 NPC
    · 수신: NPC (예: scholar_alric, turnInProfileKey)
    · distortInformation → reinterpretInfo
    · source_chain / hop_depth 규칙 적용
            │
            ▼
  interpretedFacts[]  ◄── 퀘스트·LLM·판정의 유일한 사실 소스
            │
            ├─► quest-runtime.evaluateQuestCompletion / pickOutcomeBranch
            ├─► llm-adapter + LlmFactAnchor (연출만, 브라우저)
            └─► knowledgeAudit (진단만, v1 outcome 무관)


7. 모듈 맵
--------------------------------------------------------------------------------

  7.1 파서·사전
    dictionaries.js     NpcLexicon (장소·행동 패턴·분류 키워드)
    npc-parser.js       NpcParser — DOM 없음, Node/브라우저 공용
    app.js              브라우저 UI·render·buildReadableDialogue 만

  7.2 결정론 엔진
    quest-system.js
      InfoAtom, NPC, KnowledgeBase
      distortInformation, reinterpretInfo, propagateFactBundle, propagateChain
      applyBundleCoherence, executeScenario, toGroundedFact
      compareWithGroundTruth (QA)

  7.3 시간·지식 레이어
    game-clock.js           sessionKey별 tick / day·hour
    knowledge-layers.js     WorldTruth(객관), PlayerKnowledge(접촉 지식)
    deception-audit.js      turn-in 보고 vs WorldTruth 비교 (진단)
    session-snapshot.js     RAM 세션 export/import (dev; UE save 권위)

  7.4 퀘스트·게임
    quest-runtime.js        catalog, turn-in, accept, processSteps, flow
    quest-game-state.js     gold, xp, worldFlags, questHistory (로컬 데모)
    quests-draft.json       quest_abandoned_cargo

  7.5 평판·프로필
    reputation-system.js
    npcs.json, player-profile.json, reputation-config.json
    data-loader.js (브라우저 JSON 로드)

  7.6 LLM (브라우저·Node 내부; v1 HTTP 미노출)
    llm-adapter.js          mock / OpenAI / Anthropic
    llm-fact-anchor.js      출력 수치·주어 검증
    llm-mock-fallback.js    anchor 실패 시 persona·violation별 연출
    llm-settings.js         브라우저 localStorage 설정

  7.7 HTTP (Node)
    api-bootstrap.mjs       vm 로드·registerProfiles·setQuestCatalog
    npc-api-server.mjs      HTTP v1 엔드포인트
    api-smoke.mjs           라이브 HTTP 검증
    run-api.cmd / run-api.ps1  Windows 서버 기동 (npm 불필요)


8. 정식 콘텐츠 — quest_abandoned_cargo
--------------------------------------------------------------------------------
  제목: 밀수업자의 버려진 화물
  데이터: quests-draft.json (의뢰인 3명)

  의뢰인 / quantityMode
    guard_timid      dramatic
    merchant_greedy  dramatic
    scholar_alric    faithful

  turn-in 수신: scholar_alric (의뢰인별 turnInTarget)

  기본 보고문 예:
    "안개 계곡에 밀수 화물이 버려져 있다. 마약초 열두 개가 들어 있다"

  outcome (interpreted 수량·확신 기준)
    authority_path    qty ≥ 10, certainty ≥ 0.6 → gold 150, cargo→authority
    black_market_path qty ≤ 9, certainty ≥ 0.4  → gold 300, cargo→black_market
    fallback          그 외                    → gold 80,  destination unknown


9. 지식 레이어 정책 (v1)
--------------------------------------------------------------------------------

  WorldTruth
    - 조사 완료·GM 시드용 객관 사실
    - turn-in 요청 시 worldTruthFacts 로 시드 가능

  PlayerKnowledge
    - 플레이어가 접한 interpreted 스냅샷 (direct / heard)
    - turn-in 직전 audit 후 기록 (순서 보장)

  knowledgeAudit (runQuestTurnIn 응답)
    - affectsOutcome: false  (v1 고정)
    - v1Policy: diagnostic_only
    - 게임 gold·cargo_destination 은 interpretedFacts → outcomes[] 만 사용

  API v2 예정: 기만 severity → reputation penalty, deception outcome 분기


10. source_chain · hop_depth
--------------------------------------------------------------------------------
  propagateFactBundle 시 prospective chain: […발신, 수신]
  규칙 예: source_chain_secondhand, source_chain_multi_hop, source_chain_qty_doubt
  toGroundedFact / LLM [Grounded DATA]에 source_chain, hop_depth 포함
  propagateChain: hop마다 certainty decay, chain append


11. LLM · 앵커링
--------------------------------------------------------------------------------
  입력: buildContextGrounding(interpretedFacts) → dataOnly JSON
  mock: persona별 말투, facts 고정
  live: LlmFactAnchor.validateSpeech → 실패 시 mock_fallback (facts 동일, 프레이밍만 변경)
  v1 HTTP API: LLM 엔드포인트 없음 (404 not_in_v1)

  LLM API 결제·연동은 대사 품질용. turn-in·분기·보상은 LLM 없이 완결.


12. 퀘스트 플로우
--------------------------------------------------------------------------------

  12.1 로컬·브라우저 (전체)
    acceptQuest → processSteps (dialogue → travel → fact_input)
    fact_input 시 runQuestTurnIn
    → QuestGameState.applyTurnInOutcome

  12.2 travel / interact
    quest-runtime: travel = "이동 완료" 스텁 문자열만
    실제 맵·트리거·조사 결과 → UE 책임 (v1)

  12.3 HTTP v1 (UE 권장 경로)
    POST /v1/quest/turn-in 만으로 완료 판정·보상·worldFlags 산출
    GET  /v1/quest/meta — 의뢰인·예시 보고문
    GET  /v1/quest/accept-dialogue (선택)

  12.4 giverId 의미 (UE 필수)
    giverId = 퀘스트 의뢰인 (guard_timid | merchant_greedy | scholar_alric)
    보고 수신 NPC = turnInProfileKey (대부분 scholar_alric)
    상세: INTEGRATION-GUIDE.txt §8


13. HTTP API v1
--------------------------------------------------------------------------------

  기동:
    run-api.cmd
    node npc-api-server.mjs --port=8787 [--host=0.0.0.0]

  부트 (api-bootstrap.mjs):
    dictionaries → npc-parser → quest-system
    registerProfiles(npcs.json, player-profile.json)
    ReputationSystem.setConfig(reputation-config.json)
    QuestRuntime.setQuestCatalog(quests-draft.json)
    + game-clock, knowledge-layers, deception-audit, session-snapshot

  엔드포인트:
    GET  /health
    GET  /v1/quest/meta
    POST /v1/parse
    POST /v1/quest/turn-in          ◄ UE 주력
    GET  /v1/quest/accept-dialogue
    GET  /v1/session/snapshot
    POST /v1/session/import
    *    /v1/dialogue, /v1/llm     → 404 (v2)

  오류 형식:
    { "ok": false, "error": { "code", "message", "details" } }

  turn-in 응답 핵심 필드 (UE 반영):
    completion.completed
    outcomeBranch.branchId
    outcome.rewards.gold
    outcome.worldFlags.cargo_destination
    propagation.interpretedFacts (디버그)
    knowledgeAudit (판정 X)

  authoritativeSave: unreal_engine
  serverSession: in_memory_only

  상세 스키마: GAME-API.json (version 2)
  UE 실무 절차: INTEGRATION-GUIDE.txt


14. v1 vs v2 경계
--------------------------------------------------------------------------------

  v1 포함                          v2 이후
  ─────────────────────────────────────────────────
  parse, turn-in, quest/meta       POST /v1/dialogue (LLM)
  accept-dialogue                  POST /v1/quest/flow
  session snapshot (dev)           기만 → outcome / rep
  interpretedFacts 분기            WorldTruth → 필수 조사 게이트
  knowledgeAudit 진단              서버 세이브 영속 (선택)

  UE가 v1에서 담당:
    이동·조사·3D 대사·세이브게임·시네마·HTTP turn-in


15. 구현 완료 체크리스트 (기능 단위)
--------------------------------------------------------------------------------
  [x] 결정론 왜곡·전파·KB (quest-system.js)
  [x] 한국어 파서 독립 모듈 (npc-parser.js + dictionaries.js)
  [x] 퀘스트 turn-in·accept·processSteps·flow (quest-runtime.js)
  [x] 로컬 gold / worldFlags / questHistory (quest-game-state.js)
  [x] 플레이어↔NPC 평판 tier (reputation-system.js + JSON 3종)
  [x] 프로필 v3 동기화 테스트 (profiles-sync-test)
  [x] LLM mock + live 선택 + fact anchoring + mock_fallback
  [x] GameClock / WorldTruth / PlayerKnowledge 레이어
  [x] turn-in knowledgeAudit (진단만, 분기 불변)
  [x] deception-audit.js
  [x] session-snapshot (dev용; authoritative save = UE)
  [x] HTTP API v1 (npc-api-server.mjs, api-bootstrap.mjs)
  [x] GAME-API.json, INTEGRATION-GUIDE.txt, ue-bridge/INTEGRATION.md
  [x] 브라우저 Scenario + Quest Playtest (index.html, app.js)
  [x] Node 회귀 테스트 일괄 (run-all-tests.mjs, 15 suites)
  [x] HTTP 라이브 스모크 (api-smoke.mjs, api-e2e.mjs)


16. 검증된 플레이 차이 (의뢰인)
--------------------------------------------------------------------------------
  동일 고수량 보고 (마약초 12개):
    guard / merchant (dramatic) → interpreted qty ≈ 15, authority_path
    scholar (faithful)          → interpreted qty ≈ 12, authority_path

  저수량 보고 (마약초 5개) + scholar:
    → black_market_path, gold 300

  api-smoke (HTTP 라이브):
    guard_timid + 고수량 보고 → authority_path, gold 150

  3색 브리핑: processSteps[].npcLine (의뢰인별)
  neutral 수락 대사: giver.acceptDialogue 각각 상이


17. 회귀 테스트 (Node)
--------------------------------------------------------------------------------
  명령:
    cd models/npc-test
    node run-all-tests.mjs

  스위트 (15개, run-all-tests.mjs)
  ─────────────────────────────────────────
    npc-parser-test.mjs
    deception-audit-test.mjs
    api-server-test.mjs
    distortion-test.mjs              (11)
    integration-test.mjs             (9)
    quest-test.mjs
    reputation-test.mjs
    accept-dialogue-test.mjs
    process-steps-test.mjs
    profiles-sync-test.mjs
    quest-flow-test.mjs
    knowledge-layers-test.mjs
    source-chain-test.mjs
    llm-anchoring-test.mjs
    batch-parse-quick-test.mjs       (80문장)

  설계 관점 커버리지:
    결정론 회귀: distortion, integration, quest, source-chain
    파서: npc-parser-test, batch-parse-quick (CI)
    API: api-server-test (in-process), api-smoke (HTTP 라이브)
    지식·앵커: knowledge-layers, deception-audit, llm-anchoring

  HTTP 연동 확인 (서버 실행 중):
    run-api.cmd  →  run-api-smoke.cmd
    또는 node api-smoke.mjs

  별도 실행 (시간 소요):
    node batch-parse-test.mjs --count=2000
    node api-e2e.mjs


18. 로컬에서 체험하는 방법
--------------------------------------------------------------------------------
  A) 브라우저 (전체 파이프라인 UI)
     1) index.html 을 정적 서버로 연다
     2) Quest Playtest → 의뢰인 선택
     3) Accept Quest / Next Step / Run Full Flow
     4) Game State에서 gold, worldFlags.cargo_destination 확인

  B) HTTP API (UE와 동일 계약)
     1) run-api.cmd  (또는 node npc-api-server.mjs)
     2) run-api-smoke.cmd  (연동 확인)
     3) POST /v1/quest/turn-in — INTEGRATION-GUIDE.txt §6~7
     4) GET /v1/quest/meta — 의뢰인 목록
     5) 응답 outcome → UE 세이브 반영


19. 아직 안 된 것 (로드맵)
--------------------------------------------------------------------------------
  [UE — 최우선]
    - nova/ C++·Blueprint HTTP 클라이언트 0건
    - POST /v1/quest/turn-in → gold, cargo_destination 세이브
    - 가이드: INTEGRATION-GUIDE.txt

  [게임 연출 — UE]
    - processSteps travel / interact (엔진은 스텁만)
    - introDialogue / completionDialogue 시네마
    - 아이템 수거·전달

  [API v2]
    - POST /v1/dialogue (LLM)
    - POST /v1/quest/flow (전체 processSteps)
    - 기만·조사 결과가 outcome / 평판에 반영

  [콘텐츠 확장]
    - 퀘스트 2·3본, 세 가지 색 소문 대규모 연출
    - live LLM 유료·품질 실측

  [의도적 미구현]
    - nova/ 언리얼 HTTP 클라이언트·Subsystem
    - processSteps 맵 트리거 ↔ WorldTruth 자동 동기
    - LLM을 퀘스트 실패/성공 판정에 사용
    - 서버 재시작 후 세션 자동 복구 (UE import 책임)
    - HTTP LLM proxy (v2)


20. 팀 합의 사항 (진행 시 혼동 방지)
--------------------------------------------------------------------------------
  - Authoritative save = 언리얼. API sessionKey·QuestGameState는 서버 RAM 데모.
  - v1 API: turn-in + parse 중심. LLM·travel·full flow는 v2 또는 UE 로컬.
  - knowledgeAudit / WorldTruth: v1에서 게임 판정에 쓰지 않음 (로그·디버그).
  - 퀘스트 완료 판정 = interpretedFacts only (파서 원문·플레이어 입력 직접 X).
  - giverId = 의뢰인 (보고 수신 NPC 아님). INTEGRATION-GUIDE.txt §8.


21. Git·협업
--------------------------------------------------------------------------------
  - main = origin/main, 커밋 11f385e (npc-test API v1 포함)
  - 기현 브랜치 = main 과 동일 시점 (2026-06-05)
  - UE 팀: main pull → INTEGRATION-GUIDE.txt → run-api.cmd
  - 문서: ENGINE-DESIGN / INTEGRATION-GUIDE / GAME-API


22. 주요 경로 (파일 찾기)
--------------------------------------------------------------------------------
  UE 연동 (1순위)  INTEGRATION-GUIDE.txt
  설계·진행        ENGINE-DESIGN.txt (본 문서)
  진행·테스트       run-all-tests.mjs, package.json, README.md
  API 서버           npc-api-server.mjs, run-api.cmd, GAME-API.json
  서버 빠른 시작     API-QUICKSTART.md
  퀘스트 데이터      quests-draft.json
  파서               npc-parser.js, dictionaries.js
  UI                 index.html, app.js


23. 문서 맵
--------------------------------------------------------------------------------
  ENGINE-DESIGN.txt      본 문서 — 설계·진행·로드맵·테스트 통합
  INTEGRATION-GUIDE.txt  UE 팀 연동 (Blueprint·요청·응답·트러블슈팅)
  GAME-API.json          HTTP 스키마
  API-QUICKSTART.md      서버 실행 (마크다운)
  ue-bridge/INTEGRATION.md  요약 (마크다운, INTEGRATION-GUIDE와 중복 가능)


================================================================================
  문서 역할: ENGINE-DESIGN = 설계·진행·로드맵·테스트 (통합)
            INTEGRATION-GUIDE = UE 팀이 무엇을 어떻게 붙이는가
================================================================================
